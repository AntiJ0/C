#include "stdafx.h"
#include "Part01.h"

//전처리문(전처리 -> 컴파일(번역))
//미리 번역해놓는다 
//번역을 1번만 해도 된다
//.c -> 소스파일 -> 미리컴파일된헤더를 포함할 것을 가정하고 사용
//소스, 헤더
//.c(C), .cpp(C++) -> 소스파일(헤더에서 선언된 함수들을 정의)
//.h -> 헤더파일 -> 메뉴판
//컴파일 - 소스파일 번역
//형상관리툴(버전관리툴) - Git -> GitHub
//리포지토리 -> 저장소
//원격 저장소(Git), 로컬 저장소(현재 파일)


void Part01()
{
	HelloWorld(); //함수의 호출
}

void HelloWorld()
{
	//함수 사용하는 것(함수의 호출)
	printf("Hello, World!\n");
	system("pause");
}
